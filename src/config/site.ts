import { navItems } from "./navLinks";

export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "eMart",
  description:
    "description",
  icon: "./favicon.ico",
  navItems,
};
